tunnel: 6ca42dc0-926c-4d69-ac12-e5d661c0fa9f
credentials-file: /etc/cloudflared/credentials.json

ingress:
  # ── Upload API (port 3001) ──────────────────────────────────────────────────
  - hostname: upload.nicolasrusso.ar
    service: http://minio-upload-service:3001
    originRequest:
      connectTimeout: 10s

  # ── MinIO S3 API (port 9000) ───────────────────────────────────────────────
  - hostname: files.nicolasrusso.ar
    service: http://minio:9000
    originRequest:
      connectTimeout: 30s
      tcpKeepAlive: 30s

  # Catch-all — required by cloudflared, returns 404 for unknown hostnames
  - service: http_status:404
